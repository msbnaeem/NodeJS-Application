<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <!-- css link -->
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <link rel="icon" href="/images/Comp.gif">
    <title>CSM</title>
  </head>
  <body>
    <header>
      <!-- <link rel="icon" href="index.html" > -->
      <!-- <h1 class='logo'> <a href="Comp.gif">LOGO</a></h1> -->
      <a href="/"><img src="/images/Comp.gif" class="homelogo" alt="Logo"></a>

      <nav class="userNav">
        <ul>          
          <%if(!user){%>
            <li> <a href="/users/login">Login</a></li>
            <li>|</li>
            <li> <a href="/users/new">Sign Up</a></li>
            <!-- <li>|</li> -->
          <%} else {%>
            <li> <a href="/users/logout">Logout</a></li>
            <li>|</li>
            <!-- need to implement it to have everything that one user has or owns or participates in this page -->
            <li> <a href="/users/profile">Profile</a></li>
            <li>|</li>
            <li> <a href="/connections/newConnection">Begin Connection</a></li>
          <%}%>
        </ul>
      </nav>
      
      <hr>
      
      <nav class="geneNav">
        <ul>
          <li> <a href="/connections">Connections</a></li>
        </ul>
        
      </nav>
      
      
      <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
          <div class='error'> <%=message%></div>
          <%});%>
      <%}%>

      <% if(successMessages.length > 0) {%>
        <% successMessages.forEach(message=>{%>
          <div class='success'> <%=message%></div>
        <%});%>
      <%}%>
    </header>